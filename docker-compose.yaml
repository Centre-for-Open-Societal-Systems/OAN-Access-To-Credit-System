version: "3"

services:
  frontend:
    image: ${ECR_IMAGE}
    container_name: oan_a2c_frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - VITE_API_URL=${VITE_API_URL}

volumes: {}
